# ProfilePhotoAI Frontend

React 기반 웹 클라이언트 애플리케이션

## 구현된 기능

### 핵심 컴포넌트
- **StyleSelector**: 프로필 사진 스타일 선택 (전문적인, 캐주얼, 크리에이티브, 미니멀, 커스텀)
- **ImageUploader**: 드래그 앤 드롭 이미지 업로드 (JPG, PNG, WEBP, 최대 10MB)
- **UsageQuota**: 일일 사용량 표시 및 진행률 바
- **JobStatus**: 이미지 생성 상태 추적 및 결과 표시

### 주요 기능
- 스타일별 프롬프트 자동 생성
- 커스텀 프롬프트 입력 (2000자 제한)
- 실시간 작업 상태 폴링
- 에러 처리 및 사용자 피드백
- 반응형 디자인

## 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm start

# 프로덕션 빌드
npm run build
```

## 환경 설정

`.env.example`을 복사하여 `.env` 파일 생성:

```bash
cp .env.example .env
```

필요한 환경 변수:
- `REACT_APP_API_BASE_URL`: 백엔드 API 엔드포인트
- `REACT_APP_COGNITO_USER_POOL_ID`: Cognito 사용자 풀 ID
- `REACT_APP_COGNITO_CLIENT_ID`: Cognito 클라이언트 ID

## 프로젝트 구조

```
src/
├── components/          # 재사용 가능한 컴포넌트
│   ├── StyleSelector/   # 스타일 선택
│   ├── ImageUploader/   # 이미지 업로드
│   ├── UsageQuota/      # 사용량 표시
│   └── JobStatus/       # 작업 상태
├── pages/              # 페이지 컴포넌트
│   └── GeneratePage/   # 메인 생성 페이지
├── services/           # API 서비스
│   └── api.js          # API 클라이언트
├── config/             # 설정 파일
│   └── prompts.js      # 스타일 프롬프트
└── App.jsx             # 메인 앱 컴포넌트
```

## 스타일 가이드

### 색상 팔레트
- Primary: #4A90E2 (파란색)
- Success: #7ED321 (초록색)
- Error: #D0021B (빨간색)
- Warning: #F5A623 (주황색)
- Background: #F8F9FA
- Text: #333333

### 폰트
- Pretendard (한글 최적화)
- 제목: 24px, 본문: 16px, 설명: 14px

## 개발 가이드

### 새로운 스타일 추가
`src/config/prompts.js`에서 `STYLE_PROMPTS` 객체에 추가:

```javascript
newStyle: {
  id: 'newStyle',
  name: '새 스타일',
  description: '스타일 설명',
  prompt: 'AI 프롬프트',
  icon: '🎯'
}
```

### API 엔드포인트 추가
`src/services/api.js`에 새로운 함수 추가:

```javascript
export const newApiCall = async (data) => {
  const response = await apiClient.post('/new-endpoint', data);
  return response.data;
};
```

## TODO

- [ ] AWS Cognito 인증 통합
- [ ] WebSocket 실시간 알림
- [ ] 이미지 생성 이력 페이지
- [ ] 다크 모드 지원
- [ ] 국제화 (i18n)
- [ ] 단위 테스트 추가
